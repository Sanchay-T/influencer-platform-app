Show current task status and what's next.

1. Read `agent_docs/tasks.md`
2. Show:
   - Current task + progress (X/Y checklist items done)
   - Next action
   - Backlog count
3. Check for blockers:
   - Any failing tests?
   - Any uncommitted changes that should be committed?
   - Any oversized files that need splitting?

Output a concise status report.
